version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - WSC_ENDPOINT=https://webhml.copel.nt/wsc/rest
      - WSC_USER=yyyyy
      - WSC_PASS=xxxx
      - LOG_LEVEL=DEBUG
      - AMBIENTE_LOW=local
    volumes:      
      - ./certs/store-copel.jks:/app/jks/store-copel.jks:ro
